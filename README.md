<h1>1️⃣ What is the difference between var, let, and const?</h1>
<h3>Ans: difference between var, let and const is on there type, if a variable declared on 'var' it can reassign and redclare, and this variable also hoisted. On the other hand, let variable can reassign only it does not allow his variable to redclare, and const does'n allow both declare and reassign </h3>

<h1>2️⃣ What is the spread operator (...)?</h1>
<h3>Ans: Spread operator typically use for expand a array and copy it </h3>

<h1>3️⃣ What is the difference between map(), filter(), and forEach()?</h1>

<h3>
<pre>
 map(), filter() and forEach() used to apply loop on   
 array. But they have their different use cases.
        
    map(): do any task on array and return a new array

    filter(): it need conditions to filter particular arrays and return a new array
    
    forEach(): it through loop each element on array but it does not return new array
        
</pre>
</h3>
    

<h1>4️⃣ What is an arrow function?</h1>
<h3>Arrow function ()=>{} is short form of function. It use mostly use for declare anonymous function instead of function(){}. It also can declare in variable. Arrow function also called as function expression </h3>

<h1>5️⃣ What are template literals?</h1>
<h3>
    Template string typically dynamic version of string. The symbol of template string is (`` , ${}). In this (${}) we can use essential codes inside of string.
</h3>